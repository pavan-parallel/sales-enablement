# Archive

Superseded and scratch work, kept for history. **None of this is client-facing** and none of it is linked from the Library (`index.html`) — the live site surfaces only the finals at repo root (`healthify.html`, `healthify-v2.html`, `sequifi-ds-v4.html`).

Asset references in these files were rewritten to `../../Assets/` so they still render when opened directly from here.

- **`superseded-decks/`** — earlier complete drafts of the Sequifi/Sequoia walkthrough, in chronological order: `sequifi.html` → `sequifi-v2/v3/v4.html` → `sequifi-ds.html` → `sequifi-ds-v2.html` → `sequifi-ds-v3.html`. The current Sequoia deck is `sequifi-ds-v4.html` at root.
- **`slide-fragments/`** — standalone single-slide builds that were folded into `sequifi-ds-v3.html` (atoms, molecules, components, templates, bridge, the reason/verdict/stress-test variations, etc.).
- **`labs/`** — throwaway motion/layout experiments (`sequifi-fan-lab.html`, `cohort-explorations.html`).
